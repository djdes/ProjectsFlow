// Контент и типы AI-вкладки чат-виджета. Вынесено отдельно, чтобы и оркестратор
// (HelpWidget — владеет лентой и кнопкой «Очистить»), и панель (HelpAssistantPanel —
// рендер) импортировали без циклической зависимости.

export type ChatMessage = {
  readonly id: number;
  readonly role: 'user' | 'assistant';
  readonly text: string;
  // assistant-сообщение с кнопкой «Написать в поддержку» (превью: AI пока не отвечает).
  readonly cta?: boolean;
};

// Частые вопросы про продукт — подсказки в стиле скринов, но про ProjectsFlow.
export const ASSISTANT_SUGGESTIONS: readonly string[] = [
  'Как создать проект и добавить задачи?',
  'Что такое воркер и автоматизация?',
  'Как подключить Telegram-уведомления?',
  'Как устроен финучёт по проекту?',
];

// Превью-ответ: AI пока не подключён к диспетчеру (см. план P2), поэтому помощник
// честно говорит «скоро» и уводит пользователя в рабочую поддержку.
export const ASSISTANT_PREVIEW_REPLY =
  'AI-помощник скоро заработает — он будет отвечать на вопросы про ProjectsFlow прямо здесь. ' +
  'А пока я передам ваш вопрос команде поддержки.';
