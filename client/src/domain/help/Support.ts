// Обращение в поддержку из чат-виджета. Источник определяет, откуда пришла форма:
// 'app' — залогиненный пользователь в приложении, 'landing' — аноним с лендинга.
export type SupportSource = 'app' | 'landing';

// Жёсткий потолок длины (совпадает с серверным лимитом и UI-счётчиком «x/2000»).
export const SUPPORT_MESSAGE_MAX_LENGTH = 2000;
