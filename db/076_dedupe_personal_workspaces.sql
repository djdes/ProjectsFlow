-- 076: (заглушка) — заменена на db/077.
--
-- На проде эта миграция уже помечена применённой: ранняя версия отработала как no-op
-- (дублей «Личное» не было → пустой набор → без ошибки), поэтому migrate её скипает по
-- имени и любую её логику здесь выполнять не будет. Чтобы консолидация всё-таки прошла,
-- она вынесена в db/077_consolidate_workspaces.sql.
--
-- Намеренно пустой no-op (на свежих окружениях db/077 сделает всю работу идемпотентно).
SELECT 1;
