# Inbox UX batch — план

Итерация правок «Входящих» и карточек задач по запросам пользователя (июль 2026).

## Готово (задеплоено)
- #7 Плавающий композер «В черновик…» убран во «Входящих».
- #6 Кнопки действий на карточке уменьшены (36→32px на узком/тач).
- #3a «Группировка» → «Сортировка».
- #1 Режим «Список» убран — только канбан.
- #3b Сортировка блока делегирования: дедлайн → 3 время-колонки (drag); проект/дата/приоритет → бордер-блоки.
- #3c Дата создания на карточке по наведению (сорт по дате).

## В работе
- **#2 Самоделегирование / забрать задачу себе (с бэком).** Сервер сейчас запрещает
  self-delegation (`SelfDelegationError`), а `WithdrawTaskDelegation` работает только для
  pending. Нужно: (а) серверная операция «забрать задачу обратно себе» (reclaim) — работает и
  для accepted; (б) drop карточки на СВОЮ аватарку (добавить «мой» кубик) → reclaim/делегация себе.
- **#4** Перенести фильтры (от/кому/проект) + «Сортировка» в строку с «Входящие»/«Показать
  выполненные» (поднять контролы из блока делегирования в шапку страницы; поднять состояние).
- **#5** Объединить два drag-контекста (нижняя доска Черновики/Вручную/Воркер/Готово ↔ верхние
  время-канбаны Без срока/Сегодня/Будущее) в один на уровне InboxPage: перетаскивать задачи между
  ними + бросать на аватар для делегирования; при дропе на аватар — задача уходит в без-срока/куда-то.

## Следующие (E1–E4) — после #2/#4/#5
- **E1.** При сужении окна редактирования/аналитики хлебные крошки и обложка НЕ должны меняться
  (сейчас меняются). Окно задачи не должно влиять на верстку страницы под ним. Когда окно
  достаточно широкое — вообще ничего не меняется.
- **E2.** Кнопки в окнах редактирования/аналитики не должны менять размер. Замечено: кнопка «⋯»
  (три точки) становится вытянутой — этого быть не должно. Фиксированный размер кнопок.
- **E3.** Кнопка «Перенести в проект» (в меню ⋯) — сделать нормального размера: сейчас крупный
  текст и лого; должна быть как «История версий» (тот же размер иконки/текста/паддинга).
- **E4.** Выделение задач по всему сайту:
  - При ПЕРЕМЕЩЕНИИ задачи (drag между колонками/местами) — задача подсвечивается синим и
    остаётся выделенной, пока не кликнешь в пустоту / не сделаешь другое действие (напр. переместишь
    другую).
  - Когда задача просто ОТКРЫТА (drawer) — карточка слегка синяя + синий бордер (как в Notion,
    см. скрины).
